import chHeader from './ch/header';
import chFooter from './ch/footer'
import enHeader from './en/header';
import enFooter from './en/footer'

export const ch = {
  header: chHeader,
  footer: chFooter
}
export const en = {
  header: enHeader,
  footer: enFooter
}